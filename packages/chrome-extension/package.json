{
  "name": "@aibrowser/chrome-extension",
  "version": "0.3.1",
  "private": true,
  "type": "module",
  "files": [
    "**/*"
  ],
  "scripts": {
    "build": "bun --version",
    "typecheck": "bun --version",
    "test": "node --check background.js && node --check options.js && node --check options-validation.js"
  }
}
