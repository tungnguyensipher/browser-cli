{
  "name": "aibrowser-runtime",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "bun run --filter '@aibrowser/*' build",
    "typecheck": "bun run --filter '@aibrowser/*' typecheck",
    "test": "bun run --filter '@aibrowser/*' test"
  },
  "devDependencies": {
    "@types/node": "^25.3.5",
    "@types/ws": "^8.18.1",
    "typescript": "^5.9.3"
  }
}
